first a binary file is created from the source code . 

input and output is sometimes a bottleneck in the program . 
iso:sync_with_stdio(0);
cin.tie(0);

"\n" faster than endl ( as endl alwasy caues a flush operation) 

scanf and printf are more faster than 

to read a whole line we user getline(cin,s);

to make intput and output streams go throuhg a file 

freopen("input.txt","r",stdin);
freopen("output.txt","w",stdout);

int - 32 bit 
long long 64 bit 

(a+b)mod m = (a mod m + b mod m ) mod m 
(a-b) mod m = (a mod m - b mod m ) mod m 
(a.b) mod m = (a mod m . b mod m ) mod m 

long double is more accurate 

we use the printf function to output with precession 
printf("%.9f",x);

it is risky to compare floating point numbers with == operator 

floor() downs onne integer 
ceil() roundsup one integer 

fibonacci numbers 
f(n) = f(n-1) + f(n-2)



CHAPTER 2 
TIME COMPELXITY
maximumSubArraySum.txt

CHAPTER 3
SORTING
efficent gernal soring algorithm works on 
O(nlogn)

bubble sort 
inversion ** 
merge sort ** 
couting sort ** 

User defined Structs 
Binary Search 
lower bound 
upper bound 
equal_range 


